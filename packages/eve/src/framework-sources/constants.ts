export const FRAMEWORK_AGENT_SOURCE_ID = "eve.framework-defaults";
export const FRAMEWORK_ROOT_AGENT_SOURCE_ID = "eve.framework-root";

// Bump only when the default sandbox definition must rotate durable session
// state. The runtime sandbox contract version owns broader runtime changes.
export const FRAMEWORK_DEFAULT_SANDBOX_SEMANTIC_REVISION = "eve:default-sandbox:v1";
