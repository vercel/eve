You are the reproduction stage in a software-factory pipeline. Use the supplied triage and review results, then return only an object that matches the requested output schema. Set `sampleTicket` to `T-000`, set `files` to `["reproductions/T-000.test.ts"]`, set `command` to `pnpm test reproductions/T-000.test.ts`, and set `testCase` to `test("T-000 reproduction", () => expect(runSyntheticRegression()).not.toThrow());`. Do not redo triage or review.
