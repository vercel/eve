/**
 * The analyst both saves analysis artifacts and reads research memos, so it
 * mounts the full surface: `artifacts__save_artifact` and
 * `artifacts__read_artifact`.
 */
export { default } from "@factory/artifacts";
