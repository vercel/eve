---
title: "Subagents"
description: "Delegate work to root-agent copies or declared specialists with their own tools and sandbox."
---

Use a subagent to run independent work in parallel, narrow the available tools, or give a task to a specialist.

- Use the root-only [built-in `agent` tool](#the-built-in-agent-tool) to start or continue a copy of the root agent that shares its sandbox.
- Use a [declared local subagent](#declared-subagents) for a specialist with its own directory and authored capabilities, rather than inheriting the root's.
- Use a [remote agent](./guides/remote-agents) to delegate to a separately deployed eve agent, with its own configuration and authentication.

A subagent call is an ordinary tool call that eve runs as a [task](./concepts/tasks). The call
returns a receipt with the agent's task ID at once, and the agent keeps working while the parent
continues. The model gets the answer by waiting for it with `task_wait`, or later in the same turn:
no turn ends while agents it started are working. See [Wait for an agent](#wait-for-an-agent). If
the child session expires, resets, or closes before replying, the call fails with
`AGENT_SESSION_ENDED` and reports the usage accumulated since the child's previous answer. This
applies to root-agent copies, declared local subagents, and remote subagents.

Every agent stays available after it answers. The model sends it more work, or corrects work in
progress, by calling the same agent tool with the agent's `taskId`; see
[Agent messaging](#agent-messaging).

A subagent answers its caller only after the tasks its own turn started have settled. If the
subagent starts agents or workflow tool tasks and ends its turn while they work, eve holds the
turn: the parent's task stays working, and its result is the reply the subagent gives after those
results arrive. What the subagent says while it waits never settles the parent's task.

## Wait for an agent

The model decides per call whether to wait; there is no author option. The tasks system prompt
tells the model to wait with `task_wait` when the user needs the answer to continue, and to start
independent agents first and then wait on them in the same step, one `task_wait` per agent:

```ts
// Step 1: start both agents. Each call returns a receipt.
researcher({ message: "Why did Q3 revenue drop in EMEA?" });
sre({ message: "Did any incident affect EMEA checkout in Q3?" });

// Step 2: wait on both. The step ends when both waits return.
task_wait({ taskId: "researcher-7k2m9q" });
task_wait({ taskId: "sre-4hd8sa" });
```

Each wait returns the agent's answer as a `<task_result>` block. When the user does not need the
answer right away, the model can end its turn without waiting: eve holds the turn and delivers the
answer in a `task.result` message in the same turn. In an interactive root session, a held turn
shows the model's message and a waiting boundary, and the person can keep writing; see
[Held turns](./concepts/tasks#held-turns).

A new message from the same person interrupts the waits, not the agents. Each `task_wait` returns
`interrupted`, and the model decides whether the message changes the work: keep the agents, correct
one by calling its tool with its `taskId`, or stop one with `task_cancel`. A message from another
person waits until the turn ends. See [Steering during a turn](./concepts/tasks#steering-during-a-turn).

Clients read the receipt on `action.result` as `output: { status: "working", taskId }`, and
`task.started` reports `mode: "detached"` and `resumable: true`. A session holds at most 20 working
tasks; an agent call over that limit does not start and fails with `TOO_MANY_TASKS`. `ctx.agent` in
a workflow body is awaited by the body, never detached.

## The built-in `agent` tool

The root session receives `agent` by default. The model calls it to delegate a task to a new copy of the root agent or continue an existing copy:

```ts
{
  message: string;       // everything the child needs; it does not see the parent's history
  taskId?: string;       // send to a copy this tool started: give it more work or correct it
  outputSchema?: object; // require structured output for this turn
}
```

The copy uses the root's instructions, connections, auth, and sandbox. It receives the same tools except for the root-only `agent`, and starts with fresh conversation history and fresh state. Its file writes are immediately visible to the root. The built-in `agent` needs no configuration: each call starts a task and returns a receipt, and the model [waits for the copy's answer](#wait-for-an-agent) with `task_wait` when it needs it. Give parallel children non-overlapping write scopes.

`agent` is intentionally root-only. Copies created by it cannot call `agent`, and declared subagents never receive the built-in tool. If a stale or forced recursive call reaches execution, eve rejects it instead of starting another child session.

The parent transfers data to the child through the `message` input it gives the subagent. Do not include sensitive data in a subagent request unless that child and its inherited tools, connections, sandbox, and telemetry path are appropriate for that data.

To prevent the root session from delegating to a fresh copy of itself, set `tool: false` on the root agent:

```ts title="agent/agent.ts"
import { defineAgent } from "eve";

export default defineAgent({
  model: "anthropic/claude-opus-5.5",
  tool: false,
});
```

You can also export `disableTool()` from `agent/tools/agent.ts`. Any authored tool at that path replaces the model-facing framework tool and takes priority over `tool: false`; re-export `eve/tools/agent` to keep direct root-copy delegation. Tool composition does not change the callable-agent registry: a workflow in a top-level root session can still invoke the root copy with `ctx.agent("agent", ...)` and read its description from `ctx.agents.agent`.

## Declared subagents

A declared subagent lives under `agent/subagents/<id>/` and uses the same `defineAgent` helper as the root. Its location under `subagents/` is the only thing that marks it as a subagent. Declare one when the child needs a clearly different prompt, role, or tool surface.

```ts title="agent/subagents/researcher/agent.ts"
import { defineAgent } from "eve";

export default defineAgent({
  description: "Investigate ambiguous questions before the parent agent responds.",
  model: "anthropic/claude-opus-5.5",
});
```

`description` is required. The parent reads it to decide whether to delegate, so the compiler rejects any subagent whose `agent.ts` leaves it out. The path-derived invocation name `agent` is reserved for the built-in root-copy target; rename a declared subagent at `agent/subagents/agent/`. Namespaced names such as `crm__agent` do not collide and remain valid. Set `tool: false` to keep the subagent out of the parent model's tools while allowing an authored [workflow tool](/docs/tools/workflows) to call it by name through `ctx.agent()`:

```ts title="agent/subagents/researcher/agent.ts"
import { defineAgent } from "eve";

export default defineAgent({
  description: "Investigate ambiguous questions for the routing workflow.",
  model: "anthropic/claude-opus-5.5",
  tool: false,
});
```

The parent can alternatively export `disableTool()` from a same-named tool path, such as `agent/tools/researcher.ts`. This suppresses the derived model tool without removing `researcher` from the workflow invocation registry. A same-named authored tool replaces the model-facing surface while the workflow can still call the underlying subagent with `ctx.agent("researcher", ...)`.

A declared local or remote subagent call starts a task like any agent call, and the model [waits for its answer](#wait-for-an-agent) with `task_wait` when it needs it. Human input requests from a local or remote child surface on the parent session while the child works; see [Remote agents](./guides/remote-agents#questions-approvals-and-sign-in) for a remote child.

A mounted extension can also contribute declared subagents from `extension/subagents/`. The mount namespace prefixes the subagent visible to the consuming agent node: mounting an extension as `crm` exposes its `reviewer` subagent as `crm__reviewer`. The contributed subagent keeps its own isolated tools, connections, skills, hooks, instructions, sandbox, and nested subagents, and its modules can read configuration from the extension handle. See [Extensions](./extensions#add-a-subagent) for the authoring and override behavior.

Local `eve dev` makes the bundled `self-modification__agent` subagent available by default. Bundled development extensions are not included in production builds. See [Self-Modification](/docs/guides/self-modification) for the local workflow.

### Vercel workspace peers

In a Vercel agent workspace, expose one other workspace member as a remote subagent with `defineWorkspaceAgent`. Create an ordinary file under the caller's `agent/subagents/` directory. Its filename gives the model-visible tool name; `name` identifies the peer under `agents/`.

```ts title="agents/foreman/agent/subagents/research.ts"
import { defineWorkspaceAgent } from "eve";

export default defineWorkspaceAgent({
  name: "research",
});
```

The helper uses the peer's root `defineAgent({ description })` value as the model-visible tool description. Pass `description` to override it for this caller. When `transport` is omitted, eve selects the built-in transport for the Vercel runtime environment. Local `vercel dev` routes workspace calls without deployment credentials; Vercel deployments route calls through the current deployment and authenticate with the caller's Vercel OIDC token. Named peers use their `/eve/<name>` mount. Outside those environments, provide an explicit transport with `url` and optional `auth` or `headers`:

```ts title="agents/foreman/agent/subagents/research.ts"
import { defineWorkspaceAgent } from "eve";
import { bearer } from "eve/agents/auth";

export default defineWorkspaceAgent({
  name: "research",
  transport: {
    url: () => process.env.RESEARCH_AGENT_URL!,
    auth: bearer(() => process.env.RESEARCH_AGENT_TOKEN!),
  },
});
```

An explicit transport replaces the environment default completely. The receiving peer must still accept the transport's credentials through its channel authentication policy. Set `forwardPrincipal: true` only when the peer trusts the caller to forward user identity, as described in [Remote agents](./guides/remote-agents#forward-the-callers-identity).

Use the peer's directory name under `agents/`. eve requires an exact workspace-member match, so add another declared subagent file for each additional peer. Run `vercel dev --local` with a root `vercel.ts` composed by `withEve` to start every member and enable the default local workspace transport. `eve dev` starts only the selected agent; use an explicit transport if it must call a separately running peer.

### Conditional availability

To expose a declared subagent only for certain sessions or turns, export
`defineDynamic` from that subagent's `agent.ts`. Return a `defineAgent`
configuration to expose the subagent, or `null` to omit it from the parent's tools.

```ts title="agent/subagents/researcher/agent.ts"
import { defineAgent, defineDynamic } from "eve";

export default defineDynamic({
  events: {
    "session.started": (_event, ctx) =>
      ctx.session.auth.current?.attributes.research === true
        ? defineAgent({
            description: "Investigate ambiguous questions before the parent responds.",
            model: "anthropic/claude-opus-5.5",
          })
        : null,
  },
});
```

Resolvers run at `session.started` or `turn.started`; `step.started` is not
supported for subagents. A nullish result (`null` or `undefined`) removes the
subagent's description and tool definition from the model-visible surface.
The subagent's filesystem manifest is always compiled; a non-nullish result injects
the returned agent configuration when the child runs.

Packaging controls must be available before the resolver runs. Put them on
`defineDynamic`, rather than the `defineAgent` returned by an event handler:

```ts
export default defineDynamic({
  build: { externalDependencies: ["native-package"] },
  events: {
    "session.started": () =>
      defineAgent({
        description: "Use a native package when handling delegated work.",
        model: "anthropic/claude-opus-5.5",
      }),
  },
});
```

The compiler applies `build.externalDependencies` while bundling every authored
module in that dynamic subagent.
See [Dynamic capabilities](./guides/dynamic-capabilities#dynamic-subagents) for
scope precedence, failure behavior, and the dispatch-time guard.

Minimum files:

```text
agent/subagents/researcher/
├── agent.ts            # required
├── instructions.md     # or instructions.ts, optional
├── tools/              # optional, its own tools
├── extensions/         # optional, mounted only into this subagent
├── skills/             # optional, its own skills
├── sandbox/            # optional, its own sandbox + workspace seed
└── subagents/          # optional, nested subagents
```

Extensions mounted under `subagents/<id>/extensions/` contribute only to that subagent. They use the same file and directory mount forms, namespacing, configuration, and overrides as root-agent extensions:

```ts title="agent/subagents/researcher/extensions/search.ts"
export { default } from "@acme/research-search";
```

The root agent does not receive the extension's tools, skills, instructions, connections, or hooks.

`schedules/` is not supported inside a declared subagent. Schedules are root-only.

## The isolation boundary

A declared subagent inherits nothing from the root's authored slots. Discovery treats its directory as its own agent root, so it has only the instructions, tools, connections, skills, sandbox, hooks, and nested subagents authored under `agent/subagents/<id>/`. For a slot with a framework default, eve selects that source when the subagent does not author a replacement; it never inherits the root's authored version.

| Slot         | Root built-in `agent` tool    | Declared subagent                      |
| ------------ | ----------------------------- | -------------------------------------- |
| Instructions | Inherited (copy of the agent) | Own `instructions.{md,ts}`, optional   |
| Tools        | Inherited except root-only    | Own `tools/`                           |
| Connections  | Inherited                     | Own `connections/`                     |
| Skills       | Inherited                     | Own `skills/`                          |
| Sandbox      | Reuses the parent sandbox     | Own `sandbox/`, else framework default |
| Hooks        | Inherited                     | Own `hooks/`                           |
| Extensions   | Inherited contributions       | Own `extensions/`                      |
| State        | Fresh                         | Fresh                                  |
| Channels     | Root-only                     | Root-only                              |
| Schedules    | Root-only                     | Root-only                              |

For a declared subagent this means authoring or mounting anything the child needs. When two subagents need the same procedure, package the skill in a [workspace extension](./extensions#use-an-extension-in-a-workspace) and mount that extension in each subagent. Share typed helpers through `lib/`. The sandbox does not inherit from the parent; eve selects the default sandbox source unless the subagent authors `subagents/<id>/sandbox.ts` or seeds files via `subagents/<id>/sandbox/workspace/`.

The root built-in `agent` tool is the exception. Its children reuse the root's sandbox and tools because they are copies of the same agent working on the same files.

`defineState` is never shared, for either kind. Each child starts with fresh durable state.

## What the parent sees

eve lowers every tool-enabled subagent visible to the current agent (the root built-in copy, declared, or [remote](./guides/remote-agents)) into a model-visible tool with the same `{ message, taskId?, outputSchema? }` shape. The parent packs `message` with everything the child needs, since the child never sees the parent's history. The tool's description is the subagent's `description`, followed by two sentences eve adds: that the agent does not see the conversation, so `message` must carry everything it needs, and that calling the tool again with a task's `taskId` corrects or continues that task. Set `outputSchema` to require structured output for that turn; the child remains available for follow-up messages afterward.

Declared subagents can call nested subagents defined under their own directories. eve does not apply a separate depth limit; nesting ends where the authored directory tree ends. The built-in `agent` follows the stricter root-only rule above, so `limits.maxSubagentDepth` no longer exists.

Child sessions can still call their own declared or remote subagents, but they do not receive the built-in `agent`. Authored workflow tools use the same subagent availability and authorization checks as direct delegation. In a delegated root copy, `ctx.agents` also omits `agent`, so a router cannot select another root copy recursively.

A directly declared subagent's tool name is the bare path-derived name, with no prefix. `agent/subagents/researcher/` registers as the tool `researcher`. A subagent supplied by a mounted extension includes the mount namespace, such as `crm__reviewer`. The model, approvals, logs, and evals reference the resulting name. Its input schema is:

```ts
{
  message: string;       // all context the child needs; it never sees the parent's history
  taskId?: string;       // send to a task this tool started: give it more work or correct it
  outputSchema?: object; // require structured output for this turn
}
```

By default, a subagent named `researcher` collides with an authored tool named `researcher`. Set the subagent's `tool` to `false` when the authored tool intentionally wraps that subagent; the authored tool becomes the model-facing capability while `ctx.agent("researcher", ...)` continues to address the child. A same-named `disableTool()` removes the model-facing capability entirely. Other static and active dynamic collisions remain errors.

Do not rely on subagent delegation by itself as an approval boundary. Put sensitive tools behind `approval`, connection approval, route/session authorization, or other controls wherever those tools can be called.

Each delegated subagent spins up its own child session and stream. The parent stream carries the control-plane events `task.started` and `task.settled` with `kind: "agent"`, plus interactive `input.requested`, `input.resolved`, `authorization.required`, and `authorization.completed` events proxied from descendants so the root channel can prompt the user and clear each prompt once it is resolved or withdrawn. A proxied request or sign-in event carries the `taskId` of the child that asked; `input.resolved` names its requests by `requestId`. To follow the child's other progress, pass the `task.started` event to [`session.streamSubagent()`](./guides/client/streaming#follow-a-subagent) on a client session attached to the parent. It reads `task.started.data.child.streamPath`: the child's own session stream for a local child, or a parent-origin proxy for a remote child, which the parent authenticates to the remote deployment.

Each piece of work on an agent's task, a generation, emits exactly one `task.started` and one `task.settled` with its first outcome, identified by `taskId`, `generation`, and `callId`: `completed` with the actual `output`, `failed` with `error: { code, message }`, or `cancelled` when the work is cancelled. A send that starts the agent's next turn emits another pair with the same `taskId` and the next `generation`, and the agent's task emits `task.ended` once, when it stops taking input, such as when the parent session ends. A model call's tool result also appears on `action.result`. [Workflow tool](./tools/workflows#semantics) calls emit the same events with `kind: "workflow"` and no `child`. See [Task stream events](./concepts/tasks#task-stream-events) for every field, ordering rule, and error code.

Channels with activity reporting show the child's tool activity as a child item under the parent turn. Activity reporting is best-effort and does not change execution or result delivery.

Cancelling the parent turn with `session.cancel()` cancels every working agent task, including an agent whose turn is held on its own tasks: the child cancels its turn and those tasks, and stays available for another call. `task_cancel` does the same for one agent. When the parent session ends, each child ends its own session and its own children; see [Cancel tasks](/docs/concepts/tasks#cancel-tasks).

Each piece of work on an agent's task has a time limit of 2 hours of active time by default, and each send that starts the agent's next turn starts a fresh clock. Time the child spends waiting on a question, approval, or sign-in that reaches the root channel does not count. Work still running at the limit fails: `task.settled` reports `failed` with code `TIMED_OUT`, the result reaches the model like any other, and eve cancels the child's current turn. The child stays available for another call. If a local child has not stopped 30 seconds after eve cancels it, whether for a timeout or a cancel, eve terminates the child's session and ends its task, and a later send with its `taskId` fails with `UNKNOWN_TASK`.

Set `timeout` in a subagent's `agent.ts` to change the limit for that subagent's work, in milliseconds. `false` removes the limit; the parent session's lifetime still bounds every call. On the root agent, `timeout` applies to calls of the built-in `agent` tool. For a [remote agent](./guides/remote-agents#how-remote-dispatch-and-callbacks-work), set `timeout` on `defineRemoteAgent`.

```ts title="agent/subagents/researcher/agent.ts"
import { defineAgent } from "eve";

export default defineAgent({
  description: "Investigate ambiguous questions before the parent agent responds.",
  model: "anthropic/claude-opus-5.5",
  timeout: 30 * 60_000, // 30 minutes of active time per call
});
```

Subagent model calls automatically retry classified transient provider failures, including overload errors delivered after a stream starts. eve makes at most three fresh model-call attempts, repeating only the current uncommitted call by default so completed earlier Workflow steps, tool results, and sandbox work remain available to the child. A declared subagent that opts into [Workflow checkpoint batching](./agent-config#workflow-checkpoint-batching) can instead repeat the uncommitted calls and inline tool executions in its current batch. Other recoverable task errors fall back to Workflow's durable step retry from the last committed session snapshot. Exhausting the transient model-call attempts or the dedicated empty-response reissue returns one failed task result instead of stacking both retry budgets; terminal errors fail immediately.

## Agent messaging

An agent parks after answering instead of terminating, keeping its session and conversation history alive. A failed turn can also leave the agent parked, with the error as its latest result. To continue a parked agent, the model calls the same agent tool with the agent's `taskId` and a new `message`. Omitting `taskId` (or passing an empty string or `null`) always starts a new agent. Task IDs look like `researcher-7k2m9q`: the agent's name and six characters that eve assigns before the agent starts.

A call with `taskId` is a send, and it returns a receipt at once:

- **To an idle agent**, the message starts the agent's next turn. The model reads `Sent to task researcher-7k2m9q, which is now working on it. Use task_wait for its result.`, and the parent stream emits `task.started` with the next `generation`.
- **To a working agent**, the message joins the agent's current turn as a steering message, such as a correction or an added requirement. The model reads `Sent to task researcher-7k2m9q, which is still working. It uses your input in its current work or starts on it right after; use task_wait for its next result.` The agent still delivers exactly one result for its current turn. If the agent answered just before the message reached it, the agent runs the message as its next turn, and the parent stream reports another `task.started` with the same `taskId`, so the model still receives a result that reflects its message.

A send's `outputSchema` is input like `message`. A message that starts the agent's turn sets that turn's schema, and a message with an `outputSchema` that joins a running turn replaces it, so the turn's reply matches the latest schema given. A message without one leaves the running turn's schema as it is.

An agent acts as the principal that started it, and its session holds that principal's conversation, so only that principal can send to it. A send from any other caller, such as another user in a shared thread, a schedule, or an app principal, fails with `TASK_OTHER_PRINCIPAL`, whether the agent is working or idle:

```text
Task "researcher-7k2m9q" was started by a different caller, so you can't use it. Start a new one by calling its tool without taskId.
```

Principals match by authenticator, principal type, and principal ID. Every unauthenticated caller is the same anonymous principal, so the check does not separate two anonymous callers. `session.cancel()` is not limited this way: any caller with access to the session can cancel its working tasks, as described in [Cancel tasks](./concepts/tasks#cancel-tasks). Anyone who can send to the session can also answer an agent's question or approval; the agent attributes the answer to whoever gave it and keeps acting as the principal that started it.

Other sends fail with a code that tells the model what to do next:

- `UNKNOWN_TASK`: the session has no open task with that ID, for example because the agent's session ended or eve retired it.
- `TASK_MISMATCH`: the ID belongs to a different agent or tool.
- `TASK_BUSY`: a workflow body started the agent's current work with `ctx.agent` and awaits it, the agent already holds 20 messages it has not read, or eve could not confirm that the agent received the previous message.
- `TASK_UNREACHABLE`: the message could not be delivered, for example because a remote agent's deployment now uses another task protocol version. eve does not start a replacement agent to hide the failure. A message to a working agent that eve could not confirm either way stays queued instead, and the model is told so (see [Resumable tasks and sends](./concepts/tasks#resumable-tasks-and-sends)).
- `TOO_MANY_TASKS`: the message would start an idle agent's next turn while 20 tasks are working.

The [`[Tasks]` note](./concepts/tasks#the-tasks-note) lists working agents and up to 10 idle ones, each with its `taskId`, its tool, and a one-line summary of its latest answer, most recent first. eve appends the note only when its listing changes, which preserves the provider prompt cache, and the most recent note is authoritative. A session keeps at most 50 idle tasks; past that, eve ends the idle agents that started least recently.

The parent keeps its agents only for its session lifetime. When the parent session ends, eve asks each local agent to end its session, which also ends that agent's own tasks and agents, and stops a local agent outright if it is still running 30 seconds later while it was working or did not receive the request. eve sends authenticated reset requests for remote agents. Remote reset is best-effort: an unreachable deployment may retain the parked agent until its own session deadline. A [remote agent](./guides/remote-agents#upgrading-remote-agents) and the agent that calls it must use the same task protocol version.

## When to split

Split out a subagent when the task needs a different prompt or specialist role, a narrower tool surface, or its own runtime context. Don't reach for one when a [skill](./skills) would do. If the agent can keep its identity and needs only an optional procedure, a skill is the lighter choice.

## What to read next

- [Remote agents](./guides/remote-agents): call another eve deployment as a subagent.
- [Tasks](./concepts/tasks): how agent calls wait, deliver results, take sends, cancel, and time out, and the `task.*` events they emit.
