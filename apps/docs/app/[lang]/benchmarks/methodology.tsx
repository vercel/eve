"use client";

import { ChevronRightIcon } from "lucide-react";
import { useState } from "react";

export const Methodology = () => {
  const [expanded, setExpanded] = useState(false);

  return (
    <section className="mt-6 max-w-3xl">
      <button
        aria-controls="benchmark-methodology"
        aria-expanded={expanded}
        className="flex items-center gap-2 font-medium text-gray-900 text-sm hover:text-gray-1000 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gray-1000"
        onClick={() => setExpanded((current) => !current)}
        type="button"
      >
        <span className="flex size-6 items-center justify-center rounded-md border border-gray-500">
          <ChevronRightIcon
            aria-hidden="true"
            className={`size-3.5 transition-transform ${expanded ? "rotate-90" : ""}`}
          />
        </span>
        Methodology
      </button>
      {expanded ? (
        <p className="mt-3 text-gray-700 text-sm leading-6" id="benchmark-methodology">
          Each model completes the same deterministic eve authoring tasks three times with and
          without the guidance files generated by <code>eve init</code>; success rates include every
          scheduled run, while duration, tokens, and tool calls average valid runs. Cost estimates
          apply provider public list prices to recorded token usage; Claude Code costs are based on
          one sampled run per task and treatment.
        </p>
      ) : null}
    </section>
  );
};
