---
"eve": patch
---

Run hostless `agents/` workspaces through Vercel's local service router with `eve dev`. The command starts every workspace agent, enables local `defineWorkspaceAgent` calls without deployment credentials, and opens the TUI for a selected agent without requiring a linked Vercel project. Interactive runs can upgrade an unsupported Vercel CLI before startup.
