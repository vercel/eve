---
"eve": patch
---

Run hostless `agents/` workspaces through Vercel's local service router with `eve dev`. The command generates a temporary service graph when the workspace has no Vercel configuration, opens the TUI for a selected agent, and does not require a linked Vercel project.
