---
"eve": patch
---

Forward input requests from workflow tools in remote subagents to the parent session, so background questions can be answered through the parent and the child resumes with the answer. Concurrent questions remain independently answerable across both local and remote subagent forwarding, without replacing another workflow's pending request.
