---
"eve": patch
---

In the `eve dev` TUI, Esc now steps back one prompt at a time through the team and project pickers in `/vc:auth` and `/model` (picking the wrong team and pressing Esc returns you to team selection) instead of cancelling the whole flow; Ctrl+C still cancels back to chat.
