---
"eve": patch
---

Add run-scoped required output schemas for conversation turns, including one final schema-constrained model call and a typed recoverable failure when the model still omits `final_output`.
