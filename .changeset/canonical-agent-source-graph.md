---
"eve": minor
---

Compile framework defaults, configuration, routes, native capability plans, and in-memory agents through one canonical source graph with explicit bindings and provenance. This breaking update adopts strict agent-info v3 and extension v2 semantics, validates health responses, and makes replacements, sandbox identity, and runtime inspection follow the selected compiled graph.
