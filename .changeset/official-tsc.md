---
"eve": patch
---

Use the official TypeScript 7 `tsc` compiler for eve builds, generated projects, and fixture typechecks. New projects now install `typescript@7.0.1-rc` instead of the native-preview package.
