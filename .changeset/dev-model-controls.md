---
"eve": patch
---

The dev TUI's `/model` menu now configures reasoning effort and Gateway Fast mode alongside the model id, committing all drafted changes through one atomic `agent.ts` edit. The model picker sorts the catalog by release date and validates catalog responses.
