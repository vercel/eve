---
"eve": patch
---

Align named workspace agents from `withEve` and `eve build` with `eve/next` by mounting them at `/eve/agents/<name>`. `withEve` from `eve/vercel` now also composes a root single-agent project at the unprefixed `/eve/v1/*` route.
