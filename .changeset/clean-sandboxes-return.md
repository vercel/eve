---
"eve": minor
---

Sandbox definitions now return durable `Sandbox` values directly. Provider templates are exported separately for build prewarming, provider adapters own restoration, and parent or root sandboxes can be returned explicitly for subagent sharing.
