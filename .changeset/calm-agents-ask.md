---
"eve": patch
---

Let remote subagents inherit the parent session's human-input capability so native questions and approvals can proxy through the parent channel. Trusted receivers can also read the replay-stable invocation operation ID during `onMessage` for idempotent pre-execution admission.
