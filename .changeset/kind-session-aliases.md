---
"eve": minor
---

Replace continuation.rekey() with continuation.alias(): every claimed address remains active and accepts messages through the session's merged hook pump. Steering now applies input at the next committed workflow boundary within the same turn, allowing current model and tool work to finish.
