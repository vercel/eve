---
"eve": patch
---

Add experimental opt-in Discord Gateway message ingestion to `discordChannel()`. It verifies signed relay envelopes and lets slash commands and ordinary messages share connector-scoped Discord conversation routing.
