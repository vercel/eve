---
"eve": patch
---

Add `defineWorkspaceSubagents()` for selecting workspace members as remote subagents, with explicit runtime target resolution and a `vercelWorkspaceTarget()` helper for generated Vercel member routes.
