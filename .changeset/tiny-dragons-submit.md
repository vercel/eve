---
"eve": patch
---

Add an `onViewSubmission` hook to the Slack channel for custom modal submissions. Handlers receive normalized input values and workspace-scoped Slack API access while eve-owned HITL modals retain their existing behavior.
