---
"eve": patch
---

Polish `eve init` with a fresh terminal screen that preserves scrollback, timed installation progress, and a cleaner transition into chat. Failed installs show bounded diagnostics and recovery instructions; debug logging retains package-manager output.
