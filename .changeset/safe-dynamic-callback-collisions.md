---
"eve": patch
---

Dynamic tools now isolate same-named callback implementations by session and lifecycle scope. Re-resolving the same logical tool still replaces its binding with the latest deployed code.
