---
"eve": patch
---

Stamp Workflow runs with whether hosted Agent Runs OTEL tracing is enabled so dashboards can distinguish legacy Workflow-only runs from enabled but unsampled traces. Session titles remain available for the dashboard to apply that display policy.
