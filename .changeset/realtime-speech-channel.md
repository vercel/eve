---
"eve": patch
---

Add a realtime speech channel plus React voice helpers. The channel mints AI Gateway realtime client secrets and maps finalized speech turns into durable Eve turns, while `eve/react/voice` provides a minimal microphone button and hook for browser UIs.
