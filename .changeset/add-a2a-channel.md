---
"eve": patch
---

Add an A2A 1.0 JSON-RPC channel that publishes an Agent Card and lets authenticated A2A clients start, inspect, continue, and cancel durable eve tasks. OAuth-backed channels can enforce required scopes and derive their Agent Card security declaration from existing auth configuration.
