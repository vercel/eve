---
"eve": patch
---

Export `auto()` from `eve/tools/agent-router` for custom workflow routers that want the standard JEV selection step while controlling candidate agents, instructions, or evaluation model. `agentRouter()` now also accepts optional model-ID and instruction overrides.
