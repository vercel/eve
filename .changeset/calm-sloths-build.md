---
"eve": minor
---

Allow Connect-backed Slack channels to configure separate app and bot presentation, required and optional OAuth scopes, and Events API subscriptions for generated app manifests. Replace `botName` with `app.name` and `bot.displayName`.
