---
"eve": patch
---

Give deployed self-modification sandboxes unrestricted egress while brokering read-only GitHub credentials only during checkout, reject backends without runtime credential transforms, and serialize registry installs with publication against each proposal workspace.
