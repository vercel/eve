---
"eve": patch
---

Rewrite the generated project `AGENTS.md` so coding agents route to the shipped docs before writing code, prefer registry integrations over hand-rolled ones, and use `eve link`/`eve deploy` instead of calling `vercel` directly.
