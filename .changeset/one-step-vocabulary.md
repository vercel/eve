---
"eve": patch
---

Internal: the generate harness and the durable turn step now speak one classification vocabulary — the core loop's `TurnStepResult` — instead of two translated shapes. No behavior change.
