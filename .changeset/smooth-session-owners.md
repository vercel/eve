---
"eve": patch
---

Run conversational turns directly inside one owning session workflow. Idle sessions now hand their settled state and complete hook set to the exact deployment that accepted a new delivery while preserving the original session id and event stream, and continuation rekeys retain every previously claimed address.
