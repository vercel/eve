---
"eve": patch
---

Keep dynamic skill announcements in system instructions on every turn. Sandboxes that report the filesystem root as `$HOME` now use `/workspace/skills` instead of advertising `/.agents/skills`.
