---
"eve-selfmod": patch
---

Make selfmod use parallel structured file discovery and a safe exact-replacement tool instead of shell commands, and give it the Eve authoring skill with read-only access to the installed version's documentation.
