---
"eve": patch
---

Persist changes to conversation history and session context in turn-step results instead of repeating full snapshots. Workflow replay reconstructs the state between steps; step inputs still contain the full state.
