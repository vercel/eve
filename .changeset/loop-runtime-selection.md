---
"eve": patch
---

`eve dev` accepts an experimental `--loop <inline|workflow|temporal>` flag that selects the local loop implementation. All three implementations use the same session and turn programs; Workflow remains the default, and Temporal requires its optional Worker and testing peers.
