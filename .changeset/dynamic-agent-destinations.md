---
"eve": patch
---

Register agent destinations from tools and hooks, call them from ordinary tools, and advertise them on the next eligible model request. Registered destinations survive failed calls; unknown or removed handles now fail instead of silently starting a replacement session.
