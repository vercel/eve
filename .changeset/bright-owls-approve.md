---
"eve": minor
---

Standardize authored tools and connections on an `approval` function that returns AI SDK 7 approval statuses, with synchronous and asynchronous policies supported. Boolean results remain supported as aliases for user approval and no approval, while schedules no longer accept approval configuration.
