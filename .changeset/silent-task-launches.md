---
"eve": patch
---

Add `"auto-silent"` and `"cohort-silent"` task delivery policies for sessions whose activity cards already show background work. These variants omit a redundant launch acknowledgement while retaining the corresponding result reporting behavior.
