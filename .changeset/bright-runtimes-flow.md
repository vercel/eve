---
"eve": patch
---

Run `Workflow` programs with the official AI SDK code-mode runtime while preserving eve's durable subagent accounting and event stream. Workflow programs paused across this runtime upgrade now receive an explicit retryable rerun error.
