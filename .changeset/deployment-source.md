---
"eve": patch
---

Capture the GitHub repository, immutable commit identity, and repository-relative app root during production builds. Server-side authored code can read the source through `getDeploymentSource()` when the build establishes all three values.
