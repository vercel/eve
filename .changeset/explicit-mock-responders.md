---
"eve": patch
---

Preserve explicitly configured `mockModel()` responders in step-scoped dynamic model selection when authored-model mocking is enabled, instead of substituting the generic fixture model.
