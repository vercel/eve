---
"eve": patch
---

Add `eve/nitro` integrations for embedding one eve agent in a plain Nitro 3 or TanStack Start Vite host, plus an Astro server adapter. The shared host now supports transactional development reloads and source-independent production output while preserving host routes and deployment policy.
