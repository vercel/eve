---
"eve": patch
---

Report delegated agent usage on instrumentation `action.completed` and `action.failed` events for background agent tasks. Usage includes a known `costUsd` subtotal and a `costUsdComplete` flag; blocking workflow-tool subagent calls remain outside this coverage.
