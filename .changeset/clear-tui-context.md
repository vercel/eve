---
"eve": patch
---

The dev TUI now accepts `/clear` as an alias for `/new`, terminally retiring the current session, clearing the transcript, and starting the next prompt with fresh context in the same TUI run.
