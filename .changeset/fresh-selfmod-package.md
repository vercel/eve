---
"eve-selfmod": patch
---

Publish a development-only source editing subagent kit that mounts authored agent source read-write and can edit it directly with eve's default bash and filesystem tools.
