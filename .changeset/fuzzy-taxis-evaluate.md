---
"eve": patch
---

Fix `autoModel` to use `typesafe-ai/jev` and the `/login` Gateway connection during `eve dev`, while preserving custom AI SDK default providers.
Show `dynamic model` in the TUI footer instead of a missing-connection warning for dynamic selectors.
