---
"eve": patch
---

Normalize the experimental tasks configuration once at agent resolution so task agents consistently use persistent subagent sessions without downstream components re-deriving the implication.
