---
"eve": patch
"eve-selfmod": patch
---

Add a configurable, development-only `eve-selfmod` source-editing subagent that mounts authored agent source read-write, exposes its instructions through an extension, and uses structured file tools plus the eve authoring skill to inspect and safely update source.
