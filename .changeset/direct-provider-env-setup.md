---
"eve": patch
---

The `/model` provider flow can now save direct provider API keys such as `ANTHROPIC_API_KEY` and `OPENAI_API_KEY` to `.env.local` before showing the direct-provider `agent.ts` wiring instructions.
