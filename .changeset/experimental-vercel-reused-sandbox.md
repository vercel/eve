---
"eve": patch
---

Add an experimental Vercel Dockerfile environment that lets mutually trusted eve sessions reuse one persistent native Sandbox while preserving session-owned logical sandbox identities and lifecycle methods.
