---
"eve": patch
---

Prevent generated `withEve()` service builds from replacing a co-located Next.js Build Output bundle. Next.js middleware, React Server Component, and other host outputs now remain available in Vercel deployments.
