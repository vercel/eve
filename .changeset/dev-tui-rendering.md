---
"eve": patch
---

The dev TUI renders Markdown through a real GFM parser (width-fitted tables, code blocks, task lists, links), presents known tools with semantic activity copy (`Fetch <url>`), coalesces equivalent parallel tool calls into one display row, and shows rejected tool approvals as denied instead of successful.
