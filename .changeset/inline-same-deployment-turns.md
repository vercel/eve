---
"eve": patch
---

Run ordinary channel turns directly in their same-deployment session workflow to remove the child-workflow handshake. Turns that need sleep, background work, runtime actions, or cross-deployment execution continue in the existing feature-complete child workflow without repeating completed steps.
