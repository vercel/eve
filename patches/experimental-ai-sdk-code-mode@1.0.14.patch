diff --git a/dist/runtime/manager.js b/dist/runtime/manager.js
index 91a7614d0040e1d50ad150a4532c1aa2debfec67..da34b865f4728c830a68704c52919a07aecb54bd 100644
--- a/dist/runtime/manager.js
+++ b/dist/runtime/manager.js
@@ -153,6 +153,7 @@ function startWorkerRun({ js, tools, toolExecutionOptions, options, continuation
     let totalBridgeRequests = 0;
     let inFlightBridgeRequests = 0;
     let interruptEntryIndex;
+    let interruptSettlementScheduled = false;
     const seenWorkerRequestIds = new Set();
     const bridgeLedger = cloneLedger(continuation?.ledger ?? []);
     const determinism = continuation?.determinism ?? createDeterminismState();
@@ -728,10 +729,11 @@ function startWorkerRun({ js, tools, toolExecutionOptions, options, continuation
                     }
                     if (interruptResolution !== undefined &&
                         interruptResolution.interruptId !== replayEntry.interruptId) {
-                        throw new CodeModeProtocolError(`Interrupt resolution ${interruptResolution.interruptId} does not match pending code-mode interrupt ${replayEntry.interruptId}.`, {
-                            expectedInterruptId: replayEntry.interruptId,
-                            receivedInterruptId: interruptResolution.interruptId,
-                        });
+                        // Promise.all can replay several pending sibling interrupts at
+                        // once. Preserve non-target siblings while the requested entry
+                        // resolves; the next continuation will resolve one of them.
+                        requestInterrupt(bridgeIndex - 1);
+                        return;
                     }
                     requestInterrupt(bridgeIndex - 1);
                     return;
@@ -1040,9 +1042,6 @@ function startWorkerRun({ js, tools, toolExecutionOptions, options, continuation
         if (terminalReached) {
             return undefined;
         }
-        if (interruptEntryIndex !== undefined) {
-            return undefined;
-        }
         if (resultMessage !== undefined) {
             failTerminal(new CodeModeProtocolError(`Worker sent ${message.type} after result for invocation ${invocationId}.`, {
                 invocationId,
@@ -1111,6 +1110,21 @@ function startWorkerRun({ js, tools, toolExecutionOptions, options, continuation
         interruptEntryIndex ??= entryIndex;
     }
     function settleInterruptIfReady() {
+        if (interruptEntryIndex === undefined ||
+            terminalReached ||
+            inFlightBridgeRequests > 0 ||
+            interruptSettlementScheduled) {
+            return;
+        }
+        // Let the worker message queue drain so sibling calls created in the
+        // same Promise.all join this interruption ledger before it is signed.
+        interruptSettlementScheduled = true;
+        setImmediate(bindInvocationContext(() => {
+            interruptSettlementScheduled = false;
+            settleInterrupt();
+        }));
+    }
+    function settleInterrupt() {
         if (interruptEntryIndex === undefined ||
             terminalReached ||
             inFlightBridgeRequests > 0) {
